import React, { Fragment } from "react";
import MainPage from '../mainpage/MainPage';

const Home = () => {
   return (
      <Fragment>
         <MainPage/>
      </Fragment>
   );
};

export default Home;
